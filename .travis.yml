language: 
  - objective-c

osx_image: 
  - xcode10
before_install:
   - pod cache clean --all    
   - pod update
install: true
script:
  - xcodebuild -workspace 'Essentia.xcworkspace' -scheme 'Essentia' -destination 'platform=iOS Simulator,name=iPhone 7,OS=12.0'
